import { ManagerialInterface } from '../interfaces/managerial.interface';

const MANAGERIAL_DATA: ManagerialInterface[] = [
    { id: 1, name: 'John' },
    { id: 2, name: 'Doe' }
];

export {
    MANAGERIAL_DATA
}